[pytest]
filterwarnings =
    error
    # timm imports a deprecated torch API on load. Third-party, nothing we can
    # fix here, and it fires on every run that touches the model — which would
    # train everyone to ignore warnings, including the next real one.
    ignore::FutureWarning:timm.*
